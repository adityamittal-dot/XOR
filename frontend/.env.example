# Base URL of the Django API
VITE_API_BASE=http://localhost:8000

# "Sign in with Google" (optional)
# Leave empty to hide the button entirely. Must match the backend's
# GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=
